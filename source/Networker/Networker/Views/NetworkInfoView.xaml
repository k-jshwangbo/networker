<UserControl x:Class="Networker.Views.NetworkInfoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:vm="clr-namespace:Networker.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:NetworkInfoViewModel}"
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility"/>
        <Style x:Key="FieldLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource TextFillColorSecondaryBrush}"/>
            <Setter Property="Margin" Value="0,0,12,6"/>
        </Style>
        <Style x:Key="FieldValue" TargetType="TextBlock">
            <Setter Property="Margin" Value="0,0,0,6"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="FontFamily" Value="Consolas"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <ui:Card Grid.Row="0" Margin="0,0,0,8" Padding="10">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <ui:Button Content="Refresh" Appearance="Primary"
                           Icon="{ui:SymbolIcon ArrowClockwise24}"
                           Command="{Binding RefreshCommand}"/>
                <ui:ToggleSwitch Content="Active only" VerticalAlignment="Center" Margin="20,0,0,0"
                                 IsChecked="{Binding UpOnly}"/>
            </StackPanel>
        </ui:Card>

        <!-- Error -->
        <ui:TextBlock Grid.Row="1" Text="{Binding ErrorMessage}"
                      Foreground="{DynamicResource SystemFillColorCriticalBrush}"
                      Margin="2,0,0,8" TextWrapping="Wrap"/>

        <!-- Master + detail -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="260"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Master list -->
            <DataGrid Grid.Column="0"
                      ItemsSource="{Binding AdaptersView}"
                      SelectedItem="{Binding SelectedAdapter}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      RowHeaderWidth="0"
                      HeadersVisibility="Column"
                      GridLinesVisibility="Horizontal"
                      CanUserAddRows="False"
                      SelectionMode="Single">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Adapter" Binding="{Binding Name}"   Width="*"/>
                    <DataGridTextColumn Header="Status"  Binding="{Binding Status}" Width="70"/>
                </DataGrid.Columns>
            </DataGrid>

            <!-- Detail -->
            <ui:Card Grid.Column="1" Margin="8,0,0,0" Padding="14"
                     Visibility="{Binding HasSelection, Converter={StaticResource BoolToVisibility}}">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel DataContext="{Binding SelectedAdapter}">
                        <TextBlock Text="{Binding Name}" FontSize="16" FontWeight="SemiBold"/>
                        <TextBlock Text="{Binding Description}" Margin="0,0,0,12"
                                   Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                   TextWrapping="Wrap"/>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Type"      Style="{StaticResource FieldLabel}"/>
                            <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding InterfaceType}" Style="{StaticResource FieldValue}"/>

                            <TextBlock Grid.Row="1" Grid.Column="0" Text="MAC"       Style="{StaticResource FieldLabel}"/>
                            <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding MacText}" Style="{StaticResource FieldValue}"/>

                            <TextBlock Grid.Row="2" Grid.Column="0" Text="Link speed" Style="{StaticResource FieldLabel}"/>
                            <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding SpeedText}" Style="{StaticResource FieldValue}"/>

                            <TextBlock Grid.Row="3" Grid.Column="0" Text="DHCP"      Style="{StaticResource FieldLabel}"/>
                            <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding DhcpText}" Style="{StaticResource FieldValue}"/>

                            <TextBlock Grid.Row="4" Grid.Column="0" Text="IPv4"      Style="{StaticResource FieldLabel}"/>
                            <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding IPv4Text}" Style="{StaticResource FieldValue}"/>

                            <TextBlock Grid.Row="5" Grid.Column="0" Text="IPv6"      Style="{StaticResource FieldLabel}"/>
                            <TextBlock Grid.Row="5" Grid.Column="1" Text="{Binding IPv6Text}" Style="{StaticResource FieldValue}"/>

                            <TextBlock Grid.Row="6" Grid.Column="0" Text="Gateway"   Style="{StaticResource FieldLabel}"/>
                            <TextBlock Grid.Row="6" Grid.Column="1" Text="{Binding GatewayText}" Style="{StaticResource FieldValue}"/>

                            <TextBlock Grid.Row="7" Grid.Column="0" Text="DNS"       Style="{StaticResource FieldLabel}"/>
                            <TextBlock Grid.Row="7" Grid.Column="1" Text="{Binding DnsText}" Style="{StaticResource FieldValue}"/>

                            <TextBlock Grid.Row="8" Grid.Column="0" Text="Traffic"   Style="{StaticResource FieldLabel}"/>
                            <TextBlock Grid.Row="8" Grid.Column="1" Text="{Binding TrafficText}" Style="{StaticResource FieldValue}"/>
                        </Grid>
                    </StackPanel>
                </ScrollViewer>
            </ui:Card>
        </Grid>

        <!-- Status bar -->
        <ui:Card Grid.Row="3" Margin="0,8,0,0" Padding="10,6">
            <TextBlock Text="{Binding StatusSummary}" FontWeight="SemiBold"/>
        </ui:Card>
    </Grid>
</UserControl>
