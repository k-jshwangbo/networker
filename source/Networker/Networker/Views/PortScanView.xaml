<UserControl x:Class="Networker.Views.PortScanView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:Networker.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:PortScanViewModel}"
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Input controls -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8" VerticalAlignment="Center">
            <TextBlock Text="Host:" VerticalAlignment="Center" Margin="0,0,4,0"/>
            <TextBox Width="150" VerticalAlignment="Center"
                     Text="{Binding Host, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Text="Ports:" VerticalAlignment="Center" Margin="12,0,4,0"/>
            <TextBox Width="140" VerticalAlignment="Center"
                     ToolTip="e.g. 1-1024,3389,8080"
                     Text="{Binding PortRangeText, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Text="Timeout (ms):" VerticalAlignment="Center" Margin="12,0,4,0"/>
            <TextBox Width="55" VerticalAlignment="Center" Text="{Binding TimeoutMs}"/>

            <TextBlock Text="Threads:" VerticalAlignment="Center" Margin="12,0,4,0"/>
            <TextBox Width="50" VerticalAlignment="Center" Text="{Binding MaxConcurrency}"/>

            <Button Content="Start" Width="64" Margin="12,0,0,0" Command="{Binding StartCommand}"/>
            <Button Content="Stop"  Width="64" Margin="6,0,0,0"  Command="{Binding StopCommand}"/>
            <Button Content="Clear" Width="64" Margin="6,0,0,0"  Command="{Binding ClearCommand}"/>
        </StackPanel>

        <!-- Progress + error -->
        <Grid Grid.Row="1" Margin="0,0,0,8">
            <ProgressBar Height="18" Minimum="0"
                         Maximum="{Binding Total, Mode=OneWay}"
                         Value="{Binding Scanned, Mode=OneWay}"/>
            <TextBlock Text="{Binding ErrorMessage}" Foreground="#C00000"
                       VerticalAlignment="Center" Margin="6,0,0,0"/>
        </Grid>

        <!-- Open ports grid -->
        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding OpenPorts}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column"
                  CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Port"          Binding="{Binding Port}"       Width="80"/>
                <DataGridTextColumn Header="Service"       Binding="{Binding Service}"    Width="130"/>
                <DataGridTextColumn Header="State"         Binding="{Binding StateText}"  Width="100"/>
                <DataGridTextColumn Header="Response (ms)" Binding="{Binding ResponseMs}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Status bar -->
        <Border Grid.Row="3" Margin="0,8,0,0" Padding="8,6" Background="#F2F2F2" CornerRadius="4">
            <TextBlock Text="{Binding StatusSummary}" FontWeight="SemiBold"/>
        </Border>
    </Grid>
</UserControl>
