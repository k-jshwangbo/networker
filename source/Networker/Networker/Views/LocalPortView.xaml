<UserControl x:Class="Networker.Views.LocalPortView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:Networker.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:LocalPortViewModel}"
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8" VerticalAlignment="Center">
            <Button Content="Refresh" Width="72" Command="{Binding RefreshCommand}"/>
            <Button Content="Clear"   Width="64" Margin="6,0,0,0" Command="{Binding ClearCommand}"/>

            <CheckBox Content="Listening only" VerticalAlignment="Center" Margin="16,0,0,0"
                      IsChecked="{Binding ListenOnly}"/>

            <TextBlock Text="Filter:" VerticalAlignment="Center" Margin="16,0,4,0"/>
            <TextBox Width="200" VerticalAlignment="Center"
                     ToolTip="Match process, port, PID, or service"
                     Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>

        <!-- Error line -->
        <TextBlock Grid.Row="1" Text="{Binding ErrorMessage}" Foreground="#C00000"
                   Margin="0,0,0,8" TextWrapping="Wrap"/>

        <!-- Connections grid -->
        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding ConnectionsView}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column"
                  CanUserAddRows="False"
                  CanUserSortColumns="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Proto"     Binding="{Binding Protocol}"       Width="60"/>
                <DataGridTextColumn Header="Local"     Binding="{Binding LocalEndpoint}"  Width="190"/>
                <DataGridTextColumn Header="Remote"    Binding="{Binding RemoteEndpoint}" Width="190"/>
                <DataGridTextColumn Header="State"     Binding="{Binding StateText}"      Width="110"/>
                <DataGridTextColumn Header="Service"   Binding="{Binding Service}"        Width="110"/>
                <DataGridTextColumn Header="PID"       Binding="{Binding Pid}"            Width="70"/>
                <DataGridTextColumn Header="Process"   Binding="{Binding ProcessName}"    Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Status bar -->
        <Border Grid.Row="3" Margin="0,8,0,0" Padding="8,6" Background="#F2F2F2" CornerRadius="4">
            <TextBlock Text="{Binding StatusSummary}" FontWeight="SemiBold"/>
        </Border>
    </Grid>
</UserControl>
