<UserControl x:Class="Networker.Views.SubnetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:vm="clr-namespace:Networker.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:SubnetViewModel}"
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Input -->
        <ui:Card Grid.Row="0" Margin="0,0,0,8" Padding="10">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="Subnet:" VerticalAlignment="Center" Margin="0,0,4,0"/>
                <ui:TextBox Width="280" VerticalAlignment="Center"
                            PlaceholderText="192.168.1.10/24 or 10.0.0.5 255.0.0.0"
                            ToolTip="CIDR or IP + mask. e.g. 192.168.1.10/24"
                            Text="{Binding Input, UpdateSourceTrigger=PropertyChanged}"/>
                <TextBlock VerticalAlignment="Center" Margin="12,0,0,0"
                           Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                           Text="Results update as you type."/>
            </StackPanel>
        </ui:Card>

        <!-- Error -->
        <ui:TextBlock Grid.Row="1" Text="{Binding ErrorMessage}"
                      Foreground="{DynamicResource SystemFillColorCriticalBrush}"
                      Margin="2,0,0,8" TextWrapping="Wrap"/>

        <!-- Results -->
        <DataGrid Grid.Row="2" ContextMenu="{StaticResource GridContextMenu}"
                  ItemsSource="{Binding Fields}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  RowHeaderWidth="0"
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal"
                  CanUserAddRows="False"
                  CanUserSortColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Property" Binding="{Binding Label}" Width="200"/>
                <DataGridTextColumn Header="Value"    Binding="{Binding Value}" Width="*"
                                    FontFamily="Consolas"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
