<UserControl x:Class="Networker.Views.PingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:Networker.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:PingViewModel}"
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Input controls -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8" VerticalAlignment="Center">
            <TextBlock Text="Host:" VerticalAlignment="Center" Margin="0,0,4,0"/>
            <TextBox Width="170" VerticalAlignment="Center"
                     Text="{Binding Host, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Text="Interval (ms):" VerticalAlignment="Center" Margin="12,0,4,0"/>
            <TextBox Width="60" VerticalAlignment="Center" Text="{Binding IntervalMs}"/>

            <TextBlock Text="Timeout (ms):" VerticalAlignment="Center" Margin="12,0,4,0"/>
            <TextBox Width="60" VerticalAlignment="Center" Text="{Binding TimeoutMs}"/>

            <Button Content="Start" Width="70" Margin="12,0,0,0" Command="{Binding StartCommand}"/>
            <Button Content="Stop"  Width="70" Margin="6,0,0,0"  Command="{Binding StopCommand}"/>
            <Button Content="Clear" Width="70" Margin="6,0,0,0"  Command="{Binding ClearCommand}"/>
        </StackPanel>

        <!-- Results grid -->
        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Results}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column"
                  CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="#"        Binding="{Binding Sequence}"   Width="40"/>
                <DataGridTextColumn Header="Time"     Binding="{Binding Timestamp, StringFormat=HH:mm:ss}" Width="80"/>
                <DataGridTextColumn Header="Address"  Binding="{Binding Address}"    Width="140"/>
                <DataGridTextColumn Header="Status"   Binding="{Binding StatusText}" Width="120"/>
                <DataGridTextColumn Header="RTT (ms)" Binding="{Binding RoundtripMs}" Width="80"/>
                <DataGridTextColumn Header="TTL"      Binding="{Binding Ttl}"        Width="60"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Stats bar -->
        <Border Grid.Row="2" Margin="0,8,0,0" Padding="8,6" Background="#F2F2F2" CornerRadius="4">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{Binding StatusSummary}" FontWeight="SemiBold" Margin="0,0,16,0"/>
                <TextBlock Text="{Binding Sent, StringFormat='Sent: {0}'}" Margin="0,0,16,0"/>
                <TextBlock Text="{Binding Received, StringFormat='Recv: {0}'}" Margin="0,0,16,0"/>
                <TextBlock Text="{Binding Lost, StringFormat='Lost: {0}'}" Margin="0,0,16,0"/>
                <TextBlock Text="{Binding LossPercent, StringFormat='Loss: {0:F1}%'}" Margin="0,0,16,0"/>
                <TextBlock Text="{Binding MinRtt, StringFormat='Min: {0} ms'}" Margin="0,0,16,0"/>
                <TextBlock Text="{Binding AvgRtt, StringFormat='Avg: {0:F1} ms'}" Margin="0,0,16,0"/>
                <TextBlock Text="{Binding MaxRtt, StringFormat='Max: {0} ms'}"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
